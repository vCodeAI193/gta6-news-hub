# Frontend — Backend-API (leer lassen = rein lokaler Betrieb über localStorage)
VITE_API_URL=

# Frontend — optionale externe Dienste (ohne Werte inaktiv)
VITE_ANALYTICS_DOMAIN=
VITE_SENTRY_DSN=

# Backend (server/)
PORT=8787
DB_PATH=server/data.sqlite
JWT_SECRET=bitte-in-produktion-aendern
